{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <p>{{ post.content }}</p>
    <p><em>Publié le {{ post.createdAt|date('d/m/Y à H:i') }}</em></p>
{% endblock %}
